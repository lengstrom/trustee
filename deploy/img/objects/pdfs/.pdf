import os, os.path


for root, _, files in os.walk('.'):
    for f in files:
        fullpath = os.path.join(root, f)
        print fullpath[29:-9] + '.pdf'
        os.rename(fullpath, fullpath[29:-9] + '.pdf')